-- ============================================================
-- 回滚：移除 budget_orders.ar_received_bank
-- ============================================================
-- 注意：执行前确认该列没有被业务数据填充，否则请先备份。
-- ============================================================

ALTER TABLE public.budget_orders
  DROP COLUMN IF EXISTS ar_received_bank;
